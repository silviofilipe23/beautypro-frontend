import { PaymentMethodPipePipe } from './payment-method-pipe.pipe';

describe('PaymentMethodPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PaymentMethodPipePipe();
    expect(pipe).toBeTruthy();
  });
});
